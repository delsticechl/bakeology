<section class="admin-section fade-in">
    <h2 class="admin-title">👤 Manage Users</h2>
  
    <table class="admin-table">
      <tr>
        <th>ID</th>
        <th>Name</th>
        <th>Username</th>
        <th>Admin</th>
        <th>Actions</th>
      </tr>
  
      <% users.forEach(user => { %>
        <tr>
          <td><%= user.id %></td>
          <td><%= user.name %></td>
          <td><%= user.username %></td>
          <td><%= user.isAdmin ? "✅" : "❌" %></td>
          <td>
            <% if (!user.isAdmin) { %>
              <form action="/admin/promote/<%= user.id %>" method="POST">
                <button class="admin-btn">Promote</button>
              </form>
            <% } %>
          </td>
        </tr>
      <% }) %>
    </table>
  </section>